/*
*While
 *
let capital_emprestado = 3000;
let taxas_juros = 0.02;

let contador = 1;
let parcelas_totais = 5;

while(contador <= parcelas_totais) {
    let numero_parcela = contador;
    let juros_emprestimo = capital_emprestado * taxas_juros * numero_parcela;

    console.log("A taxa de juros no periode de " + numero_parcela + " mes(es) é de R$" + juros_emprestimo + ",00");
    contador++;
}
*/

/* let paises = ["Brasil", "Suíça", "França", "Japão"];

let contador = 0;

while ( contador < paises.length ) {

    console.log(paises[contador]);
    contador++;

} */
const idade =24;
const novaIdade = idad + 1;
window.fetch;
//teste 

